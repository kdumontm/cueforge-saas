fastapi==0.109.2
uvicorn[standard]==0.27.1
gunicorn==21.2.0
pydantic==2.6.1
pydantic-settings==2.1.0
sqlalchemy==2.0.25
psycopg2-binary>=2.9.9
python-multipart==0.0.9
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2
librosa==0.10.1
# madmom et allin1 installés séparément dans le Dockerfile (nécessitent Cython + build tools)
numpy==1.26.4
# ONNX Runtime for model acceleration (2-5× speedup on CPU)
onnxruntime>=1.17.0
scipy==1.12.0
soundfile==0.12.1
mutagen==1.47.0
stripe==8.5.0
httpx==0.26.0
requests>=2.31.0
aiofiles==23.2.1
python-dotenv==1.0.1
# Compression
brotli>=1.1.0
brotli-asgi>=1.4.0
# Metadata integrations
pyacoustid>=1.2.2
musicbrainzngs==0.7.1
spotipy==2.23.0
pylast==5.3.0
email-validator>=2.1.0
# Cache (optional — fallback in-memory si Redis non configuré)
redis>=5.0.0
# Stem separation (Demucs) — installé séparément dans le Dockerfile
# avec torch CPU-only pour minimiser la taille de l'image
# diffq>=0.2.4
# demucs>=4.0.0
# 2FA (TOTP)
pyotp>=2.9.0
qrcode>=7.4.2
# Web Push Notifications
pywebpush>=2.0.0
# Notifications & Social
# (secrets est un module built-in Python, pas besoin de package externe)
# Error monitoring
sentry-sdk[fastapi]>=2.0.0
# PDF export
reportlab>=4.0.0
fpdf2>=2.7.0
# Performance optimization: Numba JIT compilation for DSP hot paths
numba>=0.59.0
# System monitoring (CPU, memory, disk) — used by admin monitoring + optimizers
psutil>=5.9.0
# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
